import React from "react";
import tw from "tailwind.macro";
import styled from "@emotion/styled";

const Skeleton = () => <SkeletonWrap></SkeletonWrap>;

const SkeletonWrap = styled.div`
  ${tw``}
`;

export default Skeleton;
